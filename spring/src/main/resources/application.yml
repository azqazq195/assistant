# common
spring:
  profiles:
    active: local

testKey: testKey

--- # local
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:mariadb://localhost:3306/assistant
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: qwe123
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create-drop

server:
  port: 8080

--- # dev
spring:
  config:
    activate:
      on-profile: dev
